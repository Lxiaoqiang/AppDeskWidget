<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WidgetDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WidgetDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget Slot Receivers (6 slots) -->
        <receiver
            android:name=".widget.WidgetSlot1Receiver"
            android:exported="true"
            android:label="Widget Slot 1">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_1_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetSlot2Receiver"
            android:exported="true"
            android:label="Widget Slot 2">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_2_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetSlot3Receiver"
            android:exported="true"
            android:label="Widget Slot 3">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_3_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetSlot4Receiver"
            android:exported="true"
            android:label="Widget Slot 4">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_4_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetSlot5Receiver"
            android:exported="true"
            android:label="Widget Slot 5">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_5_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetSlot6Receiver"
            android:exported="true"
            android:label="Widget Slot 6">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_slot_6_info" />
        </receiver>

    </application>

</manifest>